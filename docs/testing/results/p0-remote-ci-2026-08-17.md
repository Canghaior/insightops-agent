# P0 首次远程 CI 验收结果

> 日期：2026-08-17
>
> 仓库：`https://github.com/Canghaior/insightops-agent`

## 结论

远程仓库、`origin/main` 跟踪关系和 GitHub Actions 首次真实门禁均已建立。最终成功运行：

- CI Run：`31959308152`
- 提交：`6eefe2b1a82054225051fd7372477e5890d8b14e`
- frontend：SUCCESS
- backend：SUCCESS
- 运行地址：`https://github.com/Canghaior/insightops-agent/actions/runs/31959308152`

## 首次运行中发现并修复

首次 Run `31958985659` 的 frontend 已通过，backend 在执行测试前返回 exit code 126。原因是 Windows 创建的 `mvnw` 在 Git 中为 `100644`，Ubuntu Runner 无执行权限。

修复内容：

1. 将 `mvnw` 文件模式修正为 `100755`。
2. 根据 GitHub 注解将 `actions/checkout`、`actions/setup-java`、`actions/setup-node` 更新到 v5，消除 Node.js 20/旧 action 警告。
3. 推送修复后，backend 使用 PostgreSQL 18 + pgvector service container 完整执行数据库链路门禁并通过。

远程 CI 使用固定测试数据，不调用真实 DeepSeek 或在线 GitHub Release API。
