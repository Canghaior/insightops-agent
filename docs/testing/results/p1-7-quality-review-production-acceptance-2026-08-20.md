# P1.7-A 反馈复核与评测回流生产验收

## 结论

P1.7-A 于 2026-08-20 在 `https://insightops.canghaior.com` 完成部署与生产只读验收。用户答案反馈和逐条引用纠错现在可以由系统管理员复核、转换为结构化评测候选、审批后固化为数据集版本，并通过完整 RAG 评测门禁后激活。

本次上线记录：

- 提交：`b7da9bb560313a343152b43731eada7b2fac4625`
- CI Run：`32346071059`，成功
- 生产部署 Run：`32346378528`，成功
- 数据库迁移：V19 `add p1 7 quality review loop`

## 自动化门禁

- Maven `verify` 成功：139 个后端测试，0 失败、0 错误、0 跳过。
- PostgreSQL/pgvector 真实数据库链路成功：V1～V19 共 19 个迁移顺序执行；10 个数据库门禁测试全部通过。
- 反馈闭环测试覆盖：反馈读取、转候选、候选批准、不可变版本创建、无门禁拒绝激活、旧 Run 通过但最新 Run 失败时拒绝激活、最新 Run 通过后激活，以及激活版本自动进入后续默认评测。
- 前端 Vitest：11 个测试文件、25 个测试成功。
- ESLint、Vue/TypeScript 生产构建成功，共转换 1703 个模块。
- 开发与生产 Compose 配置均通过解析校验。
- GitHub Actions 的后端、前端及 server/worker/web 三个镜像任务全部成功。

## 生产验收结果

- Alpha Owner 以系统管理员身份访问 `/admin/quality` 成功，管理导航出现“质量复核”。
- “反馈复核”“评测候选”“版本门禁”三个页签均成功加载；刷新后没有接口或页面错误。
- 生产库当前为 0 条待复核反馈、0 条候选和 0 个版本，页面正确展示空态，创建版本按钮在没有已批准候选时保持禁用。
- 页面可追溯用户、会话、Run、Trace、模型、回答及官方引用；写操作由 `/api/v1/admin/quality/**` 的系统管理员权限保护。
- 版本激活只接受该 Workspace、该版本名最新一次且状态为 `PASSED` 的评测 Run；激活事务会串行化同一 Workspace 操作并退役旧激活版本。
- 为避免伪造用户评价或污染真实质量数据，本次生产验收没有创建负面反馈、代替管理员作复核判断或创建临时版本；完整写链路由真实 PostgreSQL 门禁验证。

## 当前边界

P1.7-A 没有自动把全部负面反馈加入评测集，避免噪声或恶意内容污染发布门禁。专用 Reranker、知识库自动修复、评测版本对比报表、报告导出和站外通知仍属于后续阶段。
