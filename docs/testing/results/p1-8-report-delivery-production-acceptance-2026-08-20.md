# P1.8 报告导出与站外交付生产验收

## 结论

P1.8 于 2026-08-20 在 `https://insightops.canghaior.com` 完成生产验收。用户现在可以把已完成的技术情报分析固化为不可变报告快照，在线预览并导出 Markdown/PDF；系统同时提供加密 Webhook 渠道、异步投递、重试和审计能力。

本次上线记录：

- 主功能：`97b18d618e33ffb4fa9d187838c7fe8b128ce8ff`
- 部署失败自动诊断与回滚：`001169bedc14d7ca408c797b131cf6e57b4c621e`
- Webhook Gateway Spring 构造器修复：`a2ef5f07e0b52b5c8c056a0eae76fcb89dcebf0f`
- Webhook 公网 HTTPS 校验与友好提示：`72cb38c8f46a80ec1e8d52378ea95d42217da87b`
- 最终 CI Run：`32362244974`（CI #46），成功
- 最终生产部署 Run：`32362658160`（Deploy production #13），成功
- 数据库迁移：V20 `add p1 8 reports and delivery`

## 自动化门禁

- Maven `verify` 成功：152 个后端测试，0 失败、0 错误、0 跳过。
- PostgreSQL/pgvector 真实数据库链路成功：V1～V20 共 20 个迁移顺序执行；11 个数据库门禁测试全部通过。
- 报告/交付覆盖不可变快照、Workspace 与用户隔离、幂等入队、并发租约、过期接管、指数退避、最终失败、手动重试和审计。
- Webhook 覆盖 URL 语法与公网地址策略、端点掩码、AES-256-GCM 密文、Spring 生产构造器选择和控制器 400 校验。
- 前端 Vitest：12 个测试文件、28 个测试成功。
- ESLint 成功；Vue/TypeScript 生产构建成功，共转换 1706 个模块。
- GitHub Actions 后端、前端和 server/worker/web 三个镜像任务全部成功。

## 生产验收结果

### 报告快照与页面

- Alpha Owner 登录后访问 `/reports` 成功，主导航显示“报告交付”。
- 以 `2025-08-20` 至 `2026-08-20`、三个已关注项目和四类统一事件生成“P1.8 生产验收技术情报报告”。
- 报告成功固化 18 条已完成情报、0 条高风险项；页面展示周期、项目、风险、建议、Java 影响、升级价值、行动项和 GitHub 官方证据链接。
- 历史列表刷新后仍可读取该报告，验证报告不是临时前端状态。

### Markdown 与 PDF

- Markdown 导出成功：`insightops-c251ffd1-e65b-4c63-8b07-ef088f88b19c.md`，30,676 字节；标题、周期、18 条情报和官方证据完整。
- PDF 导出成功：`insightops-c251ffd1-e65b-4c63-8b07-ef088f88b19c.pdf`，331,006 字节，共 14 页。
- PDF 文本抽取验证标题、官方证据和生成页脚均存在。
- PDF 嵌入 `NotoSansCJKjp-Regular-Identity-H` 字体；首、中、末页视觉抽查均无中文乱码、截断、重叠或页码错误。

### Webhook 与 Worker

- 页面明确声明完整端点加密保存、列表与审计只展示掩码。
- 生产复验提交 `http://127.0.0.1:8080/internal` 时，页面显示“Webhook 地址必须使用公网 HTTPS，且不能指向本机或私网。”，没有创建渠道，也不再出现通用 500 错误。
- 服务端会在进入持久化事务前按同一策略校验，并将非法地址映射为 HTTP 400；绕过前端也不能写入非公网端点。
- 最终部署同时通过 server 和 worker 健康检查；完整投递、租约、退避、重试与审计链路由真实 PostgreSQL 门禁验证。
- 因用户未提供获准接收报告的真实公网 Webhook，本次没有向第三方发送生产数据，也没有伪造一次成功送达。

## 部署异常与恢复记录

- 首次功能部署 Run `32358164449`（#9）启动失败后，Run `32359042045`（#10）恢复到上一已知健康版本。
- 部署脚本随后增加启动失败诊断和自动回滚；Run `32359450982`（#11）据此定位 `WebhookReportDeliveryGateway` 多构造器未显式选择的问题，并成功自动回滚。
- 增加 `@Autowired` 和真实 Spring 上下文回归测试后，Run `32360708576`（#12）成功上线主功能。
- 生产验收又发现不安全 Webhook 的提示为通用 500；补充前后端双层校验后，最终 Run `32362658160`（#13）成功上线并通过复验。
- 整个过程中生产服务均有明确恢复点；V20 为追加迁移，没有执行数据删除或重置。

## 当前边界

P1.8 已完成 Markdown、PDF 和通用 Webhook。邮件、RSS、飞书/Slack 等专用渠道、公开分享链接、定时报告编排和真实第三方 Webhook 送达验收仍属于后续阶段；实际外发必须由用户提供并授权目标地址。
