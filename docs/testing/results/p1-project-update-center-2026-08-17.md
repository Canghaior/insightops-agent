# P1 项目更新中心验收记录（2026-08-17）

## 范围

- Flyway V10 采集状态和用户已读表
- Worker 任务领取、GitHub Release 采集、去重、失败退避和任务审计
- 更新列表、项目筛选、未读筛选、个人已读状态和研究问答预填
- 系统管理员采集状态与立即同步入口

## 自动验证

- Maven 全量数据库门禁通过，10 个迁移在随机隔离 Schema 中成功执行。
- 数据库门禁覆盖重复采集不重复建事件，以及两个用户的已读状态互不影响。
- Worker 单元测试覆盖成功调度和 GitHub 限流退避。
- 管理端控制器测试覆盖 `SYSTEM_ADMIN` 放行与普通用户 403。
- 前端 ESLint、Vitest（5 个文件、14 项）和生产构建通过。

## 本机真实链路

- Server、Worker、Web 分别在 `127.0.0.1:18080`、`127.0.0.1:18081`、`127.0.0.1:15173` 启动成功。
- Worker 首轮领取 3 个已关注项目，成功 3、失败 0。
- GitHub 官方 API 共返回并落库 79 条 Release；快照、事件和证据记录各 79 条，产生 3 条同步任务记录。
- 登录后的更新 API 返回 79 条更新和 79 条未读，3 个项目采集状态均为 `SUCCEEDED`。

## 结论

P1.2 的最小闭环成立：关注项目 → 自动采集 → 去重落库 → 个人更新流 → 已读隔离 → 带版本进入研究。在线模型未参与本次回归，因此没有消耗 DeepSeek 额度。
