# P1.4-C 真实 RAG 验收结果

日期：2026-08-17  
环境：Windows 本机、PostgreSQL 18 + pgvector、Ollama `bge-m3`、DeepSeek `deepseek-v4-flash`

## 测试问题

> 请仅基于官方知识库，用三点说明 Spring AI 的 Embedding Model API 如何使用，并为关键结论标注 [S#] 证据。

## 结果

- Run：`a9019fbe-d9a1-490f-87fc-6a8ba31253c0`
- 状态：`SUCCEEDED`
- 本地召回候选：12 条
- 注入模型证据：6 条
- Embedding 模型：`bge-m3`
- DeepSeek 首 Token：1,191 ms
- DeepSeek 总耗时：3,437 ms
- 总 Token：2,174
- 回答包含 `[S1]` 至 `[S6]`，并展示 6 个 `docs.spring.io/spring-ai/reference/` 官方页面链接。

数据库核对：

- `retrieval_trace.run_id` 已关联该 Run；
- `knowledge_vector_search` Tool Call 为 `SUCCEEDED`；
- `agent_run.tool_rounds = 1`；
- `agent_run.citations` 保存 6 个官方 URL；
- 刷新浏览器后回答和 6 个来源均从 PostgreSQL 恢复。

## 自动化回归

- Maven 完整 `verify`：100 个测试通过，其中数据库门禁 7 个，无跳过；
- 前端 Vitest：17 个测试通过；
- ESLint：通过；
- Vue TypeScript + Vite 构建：通过。

结论：P1.4-C 最小 RAG 闭环可用，向量检索、DeepSeek 生成、官方引用、会话恢复和全链路审计均已真实验证。
