# P2.1-A/B/C 多工具编排、恢复与成本治理验收

日期：2026-08-22

状态：A/B/C 本地实现和全部自动化门禁通过；提交、CI、生产部署和线上验收在本次发布流程中继续执行。

## 已验收范围

- 同轮独立只读 Function Call 受限并行，写工具独占，节点/依赖/终态可回放。
- 显式条件 DAG、成功/失败分支、必需/可选节点、环与写节点波次校验。
- Plan revision、安全点、暂停请求、一次性检查点消费和跨 Run 证据恢复。
- 节点、工具尝试、规划 Token/成本预算耗尽后的安全降级。
- Workspace 每日/月度 Token 与成本上限、最大并发、预警和硬拒绝。
- 事务预占、幂等实际结算、取消/断连释放和审计流水。
- Owner/系统管理员成本策略与用量页面，Run 暂停/恢复和聊天检查点 UI。
- V27/V28/V29 在 PostgreSQL 18.4 上从空 Schema 完成全量迁移。

## 自动化结果

| 门禁 | 结果 |
|---|---|
| Maven 全仓 `test` | PASS：223 个测试槽位；206 通过，17 条数据库门禁按默认环境跳过 |
| PostgreSQL 全仓 `verify` | PASS：223/223，0 失败、0 跳过 |
| P2.1 PostgreSQL 门禁 | PASS：17/17；Flyway 29/29 |
| 条件图、失败分支、检查点和配额定向测试 | PASS |
| 前端 ESLint | PASS |
| 前端 Vitest | PASS：42/42 |
| 前端生产构建 | PASS |

## 当前边界

- 显式条件图只接受 Registry 工具和静态 JSON 参数；依赖结果参数由下一轮 Planner 修订生成。
- Run 执行阶段采用 Workspace 配额与预占，不等同于套餐、订单、支付、退款和发票系统。
- 在 CI、生产部署、线上登录态页面与真实模型问答完成前，不能将本记录视为生产关闭。